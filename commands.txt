编译c语言代码：

gcc sobel.c -o sobel -lm

macOS 编译要手动link一下OpenMP的Lib

clang sobel_omp.c -o sobel_omp -lm -I/opt/homebrew/opt/libomp/include -L/opt/homebrew/opt/libomp/lib -lomp

编译并采用O3优化:
gcc sobel_omp.c -o sobel_omp -lm -fopenmp -O3



登录机器：
ssh whuang25@gw.cse.cuhk.edu.hk

第三部分的编译命令，需要用完整路径才找得到编译器
/usr/local/openmpi/bin/mpicc sobel_mpi.c -o sobel_mpi -lm


jupyter notebook导出为pdf命令
jupyter nbconvert --to webpdf "report/CMSC5702_Assignment_1_report.ipynb" --output "CMSC5702_Assignment_1_report" --output-dir "report" --allow-chromium-download